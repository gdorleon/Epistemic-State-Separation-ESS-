from setuptools import setup, find_packages

setup(
    name="ess",
    version="0.1.0",
    description="Epistemic State Separation for hallucination-aware LLM generation.",
    packages=find_packages(),
    python_requires=">=3.9",
)
